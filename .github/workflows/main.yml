name: Python CI/CD

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]
    
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest selenium webdriver-manager 
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Tests
        run: |
          pytest tests/test_exercise1.py tests/test_exercise3.py --junitxml=report.xml

      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        with:
          name: test-report-${{ matrix.python-version }}
          path: report.xml

      - name: Run test
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            COLOR=2817792 # Vert Laser
            GIF="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExbmNhajE5NXlqNnJneHVlMHZwN3R5cXdxOW5rNHNvZ3k1YTVzbzloMyZlcD12MV9naWZzX3NlYXJjaCZjdD1n/5GyP41iWGBHTzmDfZx/giphy.gif"
            MSG="🌸 **J’ADORE !** C'est Versailles ici, tout est nu et libre. 🌸"
          else
            COLOR=16711680 # Rouge Alerte
            GIF="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExbmNhajE5NXlqNnJneHVlMHZwN3R5cXdxOW5rNHNvZ3k1YTVzbzloMyZlcD12MV9naWZzX3NlYXJjaCZjdD1n/kJyTAZq6OEwqr2HAf4/giphy.gif"
            MSG="🍌 **MALAISIE VAGAL !** On a perdu l'hélico dans les strass. 🍌"
          fi

          curl -X POST -H "Content-Type: application/json" \
          -d '{
            "username": "Philippe Katerine Deluxe Edition",
            "avatar_url": "https://i.imgur.com/8nS8vS4.jpeg",
            "content": "**MAINTENANT DANS TES OREILLES :**\n'"$SPOTIFY"'",
            "embeds": [{
              "title": "Notification discord test",
              "description": "'"$MSG"'\n\n**🕺 PILOTE DU COMMIT** : `${{ github.actor }}`\n**📟 VERSION** : `parfaite`",
              "color": '$COLOR',
              "image": { "url": "'"$GIF"'" },
              "footer": { "text": "✨ Philippe Katerine veille sur ton workflow mon loubard, t'as capté ✨" }
            }],
            "components": [
              {
                "type": 1, 
                "components": [
                  {
                    "type": 2,
                    "style": 5,
                    "label": "🚁 LOGS (HÉLICO)",
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                    "emoji": { "name": "🚁" }
                  },
                  {
                    "type": 2,
                    "style": 5,
                    "label": "📦 ARTIFACTS",
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts",
                    "emoji": { "name": "💎" }
                  },
                  {
                    "type": 2,
                    "style": 5,
                    "label": "🎧 ÉCOUTER KATERINE",
                    "url": "'"$SPOTIFY"'",
                    "emoji": { "name": "🎵" }
                  }
                ]
              }
            ]
          }' ${{ secrets.DISCORD_WEBHOOK }}

  release:
    needs: test
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.whl
            dist/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}