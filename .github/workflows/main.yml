name: Python CI/CD

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]
    
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest selenium webdriver-manager 
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Tests
        run: |
          pytest tests/test_exercise1.py tests/test_exercise3.py --junitxml=report.xml

      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        with:
          name: test-report-${{ matrix.python-version }}
          path: report.xml

      - name: 🕺 DISCO-NOTIFICATION (VIP LED & LOUXOR)
        if: always()
        run: |
          # Couleurs Néon et GIFs Grande Taille
          if [ "${{ job.status }}" == "success" ]; then
            COLOR=2817792 # Vert Électrique
            GIF="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExbmNhajE5NXlqNnJneHVlMHZwN3R5cXdxOW5rNHNvZ3k1YTVzbzloMyZlcD12MV9naWZzX3NlYXJjaCZjdD1n/5GyP41iWGBHTzmDfZx/giphy.gif"
            SONG="🎶 *Louxor J’adore... et je coupe le son !*"
            MUSIC_URL="https://open.spotify.com/track/4uS9N9f7YmF8kXj1XyI3Yy" # Lien vers le banger
          else
            COLOR=16711680 # Rouge Laser Alerte
            GIF="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExbmNhajE5NXlqNnJneHVlMHZwN3R5cXdxOW5rNHNvZ3k1YTVzbzloMyZlcD12MV9naWZzX3NlYXJjaCZjdD1n/kJyTAZq6OEwqr2HAf4/giphy.gif"
            SONG="🔊 *Malaise Vagal... Système en PLS !*"
            MUSIC_URL="https://www.youtube.com/watch?v=dQw4w9WgXcQ" # Petit troll de sécurité
          fi

          curl -X POST -H "Content-Type: application/json" \
          -d '{
            "username": "Philippe Katerine - Disco Service",
            "avatar_url": "https://i.imgur.com/8nS8vS4.jpeg",
            "embeds": [{
              "title": "✨ SYSTÈME PAILLETÉ V2 - TEST REPORT ✨",
              "description": "**🕺 AMBIANCE** : '"$SONG"'\n\n**🚁 UNITÉ D’ÉLITE** : `${{ github.actor }}`\n**🦾 COMPONENT** : `V2 LED-STRASS`",
              "color": '$COLOR',
              "fields": [
                { "name": "📡 Branche", "value": " `${{ github.ref_name }}`", "inline": true },
                { "name": "💎 Status", "value": " `${{ job.status }}`", "inline": true },
                { "name": "⛓️ Run ID", "value": " `${{ github.run_id }}`", "inline": true }
              ],
              "image": { "url": "'"$GIF"'" },
              "footer": { "text": "✨ Paillettes, strass et hélicos - Philippe veille sur ton flow mon loubard ✨" }
            }],
            "components": [
              {
                "type": 1,
                "components": [
                  {
                    "type": 2,
                    "style": 5,
                    "label": "🚁 VOIR L’HÉLICO (LOGS)",
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                  },
                  {
                    "type": 2,
                    "style": 5,
                    "label": "🎵 MONTER LE SON",
                    "url": "'"$MUSIC_URL"'"
                  },
                  {
                    "type": 2,
                    "style": 5,
                    "label": "📦 ARTIFACT",
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts"
                  }
                ]
              }
            ]
          }' ${{ secrets.DISCORD_WEBHOOK }}

  release:
    needs: test
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.whl
            dist/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}